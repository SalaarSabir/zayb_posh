name: zaybposh
description: ZAYB-POSH - Modern Clothing Brand E-Commerce App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI Components
  cupertino_icons: ^1.0.6

  # NOTE: Baaki packages Module 2 mein add karenge jab zarorat hogi
  # provider: ^6.1.1
  # go_router: ^13.0.0
  # http: ^1.2.0
  # shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Assets (currently empty - we'll add images later)
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Fonts (commented out for now - we'll add custom fonts in Module 2)
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700